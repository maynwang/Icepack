#==============================================================================
# Makefile macros for conda environment, GNU/Linux systems
#==============================================================================

# Preprocessor macros
CPPDEFS    := -DFORTRANUNDERSCORE ${ICE_CPPDEFS}

# Flags for the C compiler
CFLAGS     := -c -O2

# Flags for the Fortran compiler
FREEFLAGS  := -g -O0 -ffree-form
FFLAGS     := -fconvert=big-endian -fbacktrace -ffree-line-length-none

# NetCDF Fortran includes and libraries from conda
INCLDIR := $(shell nf-config --fflags)
SLIBS   := $(shell nf-config --flibs)

# Add NetCDF include flags to the compiler flags
FFLAGS += $(INCLDIR)

# Additional flags for the Fortran compiler when compiling in debug mode
ifeq ($(ICE_BLDDEBUG), true)
  FFLAGS   += -O0 -g -fcheck=bounds -finit-real=nan -fimplicit-none -ffpe-trap=invalid,zero,overflow
else
  FFLAGS   += -O2
endif

# C and Fortran compilers
SCC   := gcc
SFC   := gfortran
CC := $(SCC)
FC := $(SFC)
LD := $(FC)

# Necessary flag to compile with OpenMP support
ifeq ($(ICE_THREADED), true)
   LDFLAGS += -fopenmp
   CFLAGS  += -fopenmp
   FFLAGS  += -fopenmp
endif

